apiVersion: "acid.zalan.do/v1"
kind: postgresql
metadata:
  name: utility-database
spec:
  teamId: "utility"
  volume:
    size: 5Gi
    storageClass: nfs-client
  numberOfInstances: 1
  users:
    utilityadmin:
      - superuser
      - createdb
  databases:
    utility_db: utilityadmin
  postgresql:
    version: "15"

